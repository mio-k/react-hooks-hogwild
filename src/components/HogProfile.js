import React from "react";

function HogProfile({showProfile, setShowProfile, hog}){
    return (
    <div className="maxPigTile">
         console.log(hog)
    {/* <h3 className="pigTile h3">{hog.name}</h3>
    <img src={hog.image} />
    <ul className="ul">
        <li>{hog.weight}</li>
        <li>{hog.specialty}</li>
        <li>Greased: {hog.greased}</li>
    </ul> */}
    </div>
    )
}
export default HogProfile;